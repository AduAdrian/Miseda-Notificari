# Environment Variables
PORT=5000
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# Database Configuration for cPanel MySQL
# Replace these with your actual database credentials from cPanel
DB_HOST=server50.romania-webhosting.com
DB_USER="misedainr"
DB_PASSWORD="smGxo&9j?f#0"
DB_NAME="misedainr_notifications"

# CORS Configuration
# Comma-separated list of allowed origins (in addition to defaults)
ALLOWED_ORIGINS=https://misedainspectsrl.ro,https://www.misedainspectsrl.ro

# Email Configuration (SMTP for forgot password)
SMTP_HOST=mail.misedainspectsrl.ro
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@misedainspectsrl.ro
SMTP_PASSWORD=Kreator1234!@
EMAIL_FROM=noreply@misedainspectsrl.ro
EMAIL_FROM_NAME=Miseda Inspect SRL

# Optional: SMS Configuration (for forgot password via SMS)
# SMS_API_KEY=your_sms_api_key
# SMS_FROM_NUMBER=your_sms_number

# Note: 
# 1. Create a new database in cPanel called 'misedainr_notifications'
# 2. Replace DB_PASSWORD with your actual MySQL password from cPanel
# 3. Generate a secure JWT_SECRET (minimum 32 characters)
# 4. Copy this file to .env and update with real values
# 5. SMTP settings are configured for cPanel email account