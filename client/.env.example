# Frontend Environment Variables
# Base URL for the backend API (no trailing slash)
REACT_APP_API_URL=https://misedainspectsrl.ro

# Optionally set the deployed frontend URL if needed elsewhere in the app
# REACT_APP_FRONTEND_URL=https://misedainspectsrl.ro
